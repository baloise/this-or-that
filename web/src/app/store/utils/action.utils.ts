export const Requested = (action: string): string => `${action}Requested`;

export const Successful = (action: string): string => `${action}Successful`;

export const Failed = (action: string): string => `${action}Failed`;
